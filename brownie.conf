# Basic Bitcoin Core configuration

# Run Bitcoin Core in the background as a daemon (comment out to run in the foreground)
daemon=1

# Enable and configure RPC server for programmatic interaction
server=1
rpcuser=brownie
rpcpassword=changeme
rpcport=8332
rpcallowip=0.0.0.0/0

# Specify the network to connect to (mainnet, testnet, or regtest)
# network=mainnet

# Set a custom data directory (optional)
# datadir=/path/to/your/custom/datadir

# Add more configuration options as needed
